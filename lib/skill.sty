\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{lib/skill}[2016/04/26 Star Wars RPG Symbols SkillS]

\RequirePackage{tikz}
\usetikzlibrary{positioning}
\RequirePackage{scalerel}
\RequirePackage{background}

\newlength{\skillheight}
\settoheight{\skillheight}{A}

\definecolor{activetalent}{RGB}{82,34,127}
\definecolor{passivetalent}{RGB}{114,205,220}

\newcommand{\talent[3]}{\noindent
\begin{tikzpicture}[samples=200]
  \newcommand\length{15em}
  \newcommand\width{15em}
  \newcommand\marge{0.2em}
  \newcommand\title{2em}

  \coordinate (a) at (0em,0em);
  \coordinate (b) at (\length,\width);
  \coordinate[below left = \marge and \marge of b] (c) {};
  \coordinate[below left = \title and (\width - (\marge * 2) ) of c] (d) {};

  \draw [fill=white, ultra thick] (a) rectangle (b);
  \draw [fill=\ifnum#1>1 blue\else red\fi] (c) rectangle (d);

  \node[above right] at (d) {\color{white}\uppercase{#2} };
  \node[below right, text width=\length] at (d) {\color{black}\uppercase{#3} };

\end{tikzpicture}
}

